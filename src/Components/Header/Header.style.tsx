import { Typography } from '@subzero/polar/build/components';
import { $WHITE } from '@Constants/color';
import styled from 'styled-components';

export const WhiteTypography = styled(Typography)`
  color: ${$WHITE}
`;
